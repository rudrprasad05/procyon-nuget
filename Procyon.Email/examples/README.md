# Procyon.Email examples

An executable example application is intentionally deferred to a later task.

This directory exists to reserve the examples location and document that no runnable example should be built as part of the initial architecture scaffolding.
